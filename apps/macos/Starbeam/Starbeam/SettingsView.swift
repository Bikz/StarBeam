import SwiftUI

struct SettingsView: View {
  var body: some View {
    Form {
      Section("Server") {
        Text("Settings UI coming next.")
          .foregroundStyle(.secondary)
      }
    }
    .padding(16)
  }
}

#Preview {
  SettingsView()
}
