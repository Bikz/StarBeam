# @starbeam/web

Next.js web dashboard for Starbeam.

## Local Development

From the repo root:

```bash
pnpm --filter @starbeam/web dev
```

Environment variables live at the repo root (`.env.example`).

